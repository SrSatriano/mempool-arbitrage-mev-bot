[package]
name = "mempool-arbitrage-mev-bot"
version = "0.1.0"
edition = "2021"
description = "Educational MEV concepts — testnet only"

[dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
# ethers = "2.0"  # habilitar para testnet real
